
.gitignore

CXXFLAGS = -Wall -g

LDFLAGS = -L/usr/lib/llvm -lclang
LDFLAGS += -lQtCore

CXX = g++

!srcToObj = |> $(CXX) $(CXXFLAGS) -c -o %o %f |> %B.o
!objsToBin = |> $(CXX) $(LDFLAGS) -o %o %f |>
!objsToAr = |> ar crs %o %f |>

