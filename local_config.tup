
# git notes:
#  - changes to this file are ignored using:
#     `git update-index --assume-unchanged local_config.tup`
#  - to un-ignore:
#     `git update-index --no-assume-unchanged local_config.tup`

# define these if needed
#PROTOC = 
#MOC = 

# define extra includes/library paths as needed
#INCLUDES +=
#LDFLAGS +=
